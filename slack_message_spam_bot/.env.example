# Copy to .env and fill with your Slack credentials
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Required when running via Socket Mode (recommended)
SLACK_APP_TOKEN=xapp-your-app-level-token

# Optional: only needed if you expose an HTTP endpoint instead of Socket Mode
# SLACK_SIGNING_SECRET=your-signing-secret

# Optional path override for channel groups file
# CHANNEL_GROUPS_FILE=channel_groups.json

# Optional: restrict who can trigger commands (comma-separated user IDs)
# REQUIRED IF INSTALLED ORGANIZATION WIDE
# ALLOWED_USERS=U123ABC,U456DEF
